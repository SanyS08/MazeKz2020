@model GrasmikMazeModel
<div>
    <table>
@for (int i = 0; i < Model.MainMaze.Height; i++)
{
        <tr>
            @for (int j = 0; j < Model.MainMaze.Width; i++)
            {
            <td>@i , @j </td>
            }
        </tr>
}

    </table>
</div>